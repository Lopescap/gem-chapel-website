# Memory — August 31, 2026

## Summary of Work Done

### Branches Section Overhaul
- Replaced the 7 old branch cards (Asokwa, Dome, Tema, etc.) with 20 new cards from the client's list
- Updated section heading to "Branches Across Ghana & Beyond" to cover the UK branch
- Made each card a clickable link to `/branches/{slug}` pages
- Created 20 individual branch page templates with nav, footer, hero, detail placeholder, and full branch quick-nav grid
- Added photos and lorem ipsum to /branches/kwanwoma, /branches/university-of-ghana, /branches/techiman
- Added Gm International logo (globe/dove/flame) to all 20 branch pages between the "All Branches" link and the title
- Added `vercel.json` for clean URL support (no .html extension)
- The photo filenames with `&` were renamed to safe slugs

### About Page
- Created `/about` page with full ministry history: vision, Dunkwa-on-Offin crusades, 1989 establishment in K.O.-Adum classroom, move to Asokwa, present HQ
- Sections: The Beginning, Establishment, Branches (51), Activities, Goals (7 items), Partner CTA
- Added "Read More About Our Story →" CTA to homepage About section before goal cards
- Added full-width cover photo above "The Beginning" section

### Books Section (Homepage)
- Replaced 5 old gradient-based book cards with 6 real cover image cards:
  1. The Names & Appellations of God — Stephen & Ebenezer Owusu-Ansah
  2. How to Study Effectively and Pass Exams — Mrs. Beatrice Owusu-Ansah
  3. Understanding Fatherhood & Sonship in the Lord — Stephen & Ebenezer Owusu-Ansah
  4. Decisions — Stephen & Ebenezer Owusu-Ansah
  5. Choosing the Right Mate — Kwame & Beatrice Owusu-Ansah
  6. All About Homosexuality and Lesbianism — Kwame & Beatrice Owusu-Ansah
- Added 7th book: The Cure for Anger — Stephen & Ebenezer Owusu-Ansah
- Linked "Understanding Fatherhood & Sonship" to Amazon: `https://amzn.to/4xxzIsH`

### Logo Replacement
- Replaced text "GEM" logo in nav and footer with actual logo image (`/media/gem-logo.png`) across all 22 pages
- Added CSS for logo sizing (42px nav, 34px scrolled, 54px footer)

### Leadership Pages
- Created 4 leadership pages linked from homepage:
  - `/leadership/Kwame-and-Beatrice-Owusu-Ansah` (with photo)
  - `/leadership/Stephen-Owusu-Ansah` (with photo)
  - `/leadership/Ebenezer-Owusu-Ansah`
  - `/leadership/Jonathan-Owusu-Ansah`
- Added 2 new leadership cards with "Bio Coming Soon" pages:
  - Rev. Enoch Ampratwum, Regional Overseer, Tema
  - Rev. John Manu, Regional Overseer, Central Region
- Made all homepage leadership cards clickable links
- Updated new leadership avatar gradients to orange/gold
- Updated all social link URLs across the site

### Article Pages
- Created 4 article pages from the provided documents:
  - `/articles/three-major-decisions-in-life`
  - `/articles/sleeping-giant`
  - `/articles/you-carry-something-precious`
  - `/articles/how-to-hear-from-God`
- Made homepage article cards clickable links to their pages

### Fresh Fire Section
- Added "Fresh Fire for Today" section to homepage Resources area above Featured Articles
- Created `/resources/fresh-fire` page (placeholder for devotional content)

### Pre-compaction flush note
This is all the work done in the August 31, 2026 session. The site is at `https://gem-chapel-website.vercel.app/`, repo is `Lopescap/gem-chapel-website`.
### Fresh Fire Build System (generator + 92 entry pages)
- Created `tools/build-fresh-fire.py` — Python 3 generator, zero dependencies, reads `tools/fresh-fire-bundle.json` as single source of truth
- Created `tools/templates/head.txt`, `tools/templates/nav.txt`, `tools/templates/footer.txt` — extracted verbatim from `articles/three-major-decisions-in-life.html` with `{{TITLE}}`, `{{META_DESC}}`, `{{CANONICAL_URL}}`, `{{OG_DESC}}`, `{{OG_TYPE}}` placeholders
- `tools/fresh-fire-bundle.json` — 305KB bundle with 92 entries, taxonomy (4 facets: theme, practice, need, attribute), derived indexes (scripture_books, series)
- Updated `collection.origin` from placeholder to `https://gem-chapel-website.vercel.app`
- Generated 92 entry pages in `resources/fresh-fire/` (e.g. `loving-god.html`, `the-faith-that-wins-part-3.html`)
- Each page has: `<h1>`, "In short" summary, key scripture blockquote (omitted when empty), blocks (p/h2), Prayer/Confession, Scriptures Referenced list, Series siblings, Theme/Need tag chips, Prev/Next by order
- All text HTML-escaped, no dates, no JSON-LD, no content alteration
- 75 links will 404 until index pages are generated (49 scripture books + 16 themes + 10 needs)
- `father` attribute and `giving` practice are reserved terms with 0 usage — not errors
- All work on branch `feat/fresh-fire` (not merged to main)

### Generator stripped to entry pages only
- `build_hub_page`, `build_sitemap`, `build_llms_txt` functions and calls removed from generator
- `sitemap.xml`, `llms.txt`, `fresh-fire-search.js` deleted from `resources/fresh-fire/`
- `resources/fresh-fire/index.html` reverted to branch-point (untouched)
### Bundle v2 + generator rewrite (fresh-fire-bundle-2.json → fresh-fire-bundle.json)
- Replaced bundle with corrected v2: `1-saiah`→`isaiah`, `ge`→`genesis`, `2-co`→`2-corinthians` fixed; `indexes.scripture_books` now 47 (was 49)
- Every facet term now has `has_page` boolean field
- `collection.url_patterns` defines canonical paths for all page types — generator now uses `resolve_url()` as single source
- Generator rewritten with `resolve_url(pattern, origin, slug=None)` — no hand-constructed paths anywhere
- Tag chips: `<a>` when `has_page=true`, `<span>` when `has_page=false` — only 8 themes and 3 needs will ever have pages
- All 47 scripture books still link (all get pages)
- Attribute tags never link individually (single combined page later)
- Regenerated 92 entries — validation passed, no placeholder residue
- 404 list now 58 links (47 scripture books + 8 themes + 3 needs), down from 75
- `father` and `giving` unchanged — reserved terms with zero usage, not errors
**This is a continuation append — do not delete prior entries.**

### Hub index pages generated (59 total)
- 8 theme index pages: `resources/fresh-fire/theme/{slug}.html` (destiny, diligence, faith, glory, knowing-god, perseverance, prayer, provision)
- 3 need index pages: `resources/fresh-fire/need/{slug}.html` (opposition, growth, purpose-search)
- 47 scripture index pages: `resources/fresh-fire/scripture/{slug}.html` (one per Bible book)
- 1 `names-of-god.html` page — all 9 attribute entries on one page (avoids thin content per name)
- Generator updated to build all index pages: `build_term_index(term_type, slug, data, entries, origin, head, nav, footer, patterns)` function
- Each index page: hero, back link to hub, `h1` with term label, optional definition, `ff-entry-list` of matching entries, back link

### Hub page rewritten (resources/fresh-fire/index.html)
- Kept hero + search input + `fresh-fire-search.js` script tag
- Replaced empty results div with all 92 entries as inline `<a>` tags (title bold + summary below)
- Added nav block linking themes/needs/scriptures/names-of-god index pages
- All 92 entries present in raw HTML markup before any `<script>` — no JS dependency for crawler visibility

### Link checker (tools/check-fresh-fire-links.py)
- Standalone Python 3 script, zero deps, walks filesystem
- cleanUrl-aware: maps `/resources/fresh-fire` → `index.html`, all others → `+.html`
- Exits 0 if all targets exist, 1 if broken links
- Proven to detect failures: renamed `psalm.html` → 26 broken links reported; renamed back → clean

### Internal links switched to root-relative
- `resolve_url()` split into `href_url(pattern, slug)` → root-relative (e.g. `/resources/fresh-fire/loving-god`) and `abs_url(pattern, origin, slug)` → absolute with origin (canonical/OG only)
- All `<a href>` tags in page content now root-relative — survives domain change and preview deploys
- Added `check_absolute_hrefs` post-check: fails if any `href="https://..."` found in page content (canonical/OG exempted)
- Regenerated all 152 pages — passed checks including "No absolute hrefs in page content"
- Proven via rename-proof: `psalm.html` removed → checker reports 26 failures

### Merge feat/fresh-fire to main
- Fast-forward merge, no conflicts. Branch kept as `feat/fresh-fire`.
- Pushed to `origin/main` — production deploy triggered
- Live verification on `https://gem-chapel-website.vercel.app`:
  - `/resources/fresh-fire` → 200
  - `/resources/fresh-fire/theme/faith` → 200
  - `/resources/fresh-fire/the-faith-that-wins-part-3` → 200
  - `/resources/fresh-fire/scripture/psalm` → 200
  - `/resources/fresh-fire/names-of-god` → 200
- `X-Robots-Tag` absent from production responses
- Hub confirmed: 92 entry links in raw HTML before `<script>`, 93 total hrefs (92 entries + self-link)
- Live link checker: 152 URLs all 200

### Hub entry list restyled
- Removed bullets (`list-style: none`)
- Bold white titles in `--font-display` at 1.15rem
- Regular weight muted-cream descriptions at 0.95rem
- Orange hover color (`--flame-orange: #E8751A`) on title, description, and arrow
- `→` arrow fades in from the right on `<li>` hover
- Gold-tinted bottom rule on each item
- CSS added to `styles.css` under `/* ─── FRESH FIRE ENTRY LIST ─── */`

### Transparent logo
- Uploaded `Untitled design-2.png` had white background — stripped to transparent PNG saved as `media/gem-logo.png`
- Replaced `gem-logo.jpg` → `gem-logo.png` across all 184+ HTML pages using `sed -i ''` replacement
- No more visible background rectangle in nav or footer

### Vercel Deployment Protection
- Preview deploys for `feat/fresh-fire` intercepted all requests with 302 → Vercel SSO login
- Cannot disable via tooling (hard bans forbid `vercel` CLI, no dashboard credentials)
- User needs to disable in Vercel project dashboard or generate bypass URL

## Key People & Roles
- **User/Client**: `reedrich` — site owner for GEM Chapel, makes content and design decisions
- **Admin/Operator**: Runs the systems I live in — reported to when issues are out of scope (Vercel protection, CLI auth)
- **Paula-Gems (me)**: AI agent for `https://gem-chapel-website.vercel.app/`

## Tech Stack
- Vanilla HTML/CSS/JS — no framework
- Vercel deployment from `Lopescap/gem-chapel-website` GitHub repo
- Vercel `cleanUrls: true` for no-.html URLs
- Python 3 generator (standard library only) at `tools/build-fresh-fire.py`
- Bundle JSON at `tools/fresh-fire-bundle.json` — single source of truth for 92 entries
- Templates at `tools/templates/{head,nav,footer}.txt` with `{{PLACEHOLDER}}` syntax
- Link checker at `tools/check-fresh-fire-links.py`
- Styles: `styles.css` with CSS custom properties (`--flame-gold`, `--flame-orange`, etc.)

## Commit Log Reference
```
b1867d3 — Add Fresh Fire generator, templates, and content bundle (tools/)
ef3b91a — Generate 92 Fresh Fire devotional pages (entry pages)
df39bef — Update generator for v3 bundle, add index pages and hub, add link checker (tools/ update)
4a0144c — Regenerate 92 entry pages, add 59 index pages, rewrite hub (full output)
a7488f6 — Switch internal hrefs to root-relative; add absolute-href check to generator
54bcee6 — Regenerate all FF pages with root-relative hrefs
75a6408 — Update gem-logo.jpg → gem-logo.png across 184 pages
d4f8ea6 — Add ff-entry-list CSS styling
```